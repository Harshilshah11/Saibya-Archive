# Web app config. Copy to .env.local.
# The web app holds no AWS keys and no database: everything comes from the Server (server branch).

# Server base URL. Default: the EC2 Server. For a Server running locally: http://localhost:3100
# Read when `npm run dev` / `npm run build` starts, so restart after changing it.
API_URL=http://43.204.46.19
